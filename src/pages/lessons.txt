// Every single state change follows this:
dispatch({ type: 'ADD_ITEM', payload: product })
//  ↓
// reducer(currentState, action) → newState
```

---

### Lesson 8 — useRef, useMemo & useCallback
**Topic:** Refs, Performance Hooks, Avoiding Unnecessary Work

**Concepts Covered:**
```
useRef:
  - Accessing DOM elements directly
  - Persisting values without re-render
  - useState vs useRef decision guide

useMemo:
  - Memoizing expensive calculations
  - When to use (and when NOT to)
  - The cost of premature optimization

useCallback:
  - Memoizing functions
  - Referential equality problem
  - When child re-renders are the problem
```

**Karigar Co. Built:**
```
Search with useRef (focus input on page load)
Price filter with useMemo 
  (expensive sort/filter calculation only reruns when products change)
ProductCard with useCallback
  (stable function reference passed as prop)
Image gallery with useRef (scroll control)
```

**Why You Won't Forget:**
> *"useMemo remembers a RESULT. useCallback remembers a FUNCTION. useRef remembers a VALUE without causing re-renders. All three are about avoiding unnecessary work."*

---

### Lesson 9 — Custom Hooks
**Topic:** Extracting & Reusing Logic

**Concepts Covered:**
```
- What is a custom hook?
- The naming rule (must start with "use")
- Extracting logic from components
- Custom hooks can use other hooks
- When to create a custom hook
- Custom hook patterns used in industry
- Testing custom hooks (concept)
```

**Karigar Co. Built:**
```
useFetch(url)        → reusable data fetching with loading/error
useCart()            → cart operations abstracted
useLocalStorage(key) → persist state in localStorage
useDebounce(value)   → debounce search input
useWindowSize()      → responsive breakpoints in JS
useOnClickOutside()  → close dropdown when clicking outside
```

**Why You Won't Forget:**
> *"Custom hooks are just functions that happen to use React hooks inside. They're how you copy-paste logic WITHOUT copy-pasting code. Extract once, use everywhere."*

---

### Lesson 10 — Forms & Validation
**Topic:** Controlled Forms, Validation, User Input

**Concepts Covered:**
```
- Controlled vs Uncontrolled components
- Single vs multiple field state
- Form submission and preventDefault
- Client-side validation (manual)
- Error messages and UX
- React Hook Form library (industry standard)
- Form accessibility (labels, aria)
```

**Karigar Co. Built:**
```
Checkout form (full):
  - Name, email, phone
  - Shipping address
  - Validation on all fields
  - Error messages under each field
  - Submit disabled until valid

Newsletter signup form
Login/Register modal (UI only, no backend yet)
```

**Why You Won't Forget:**
> *"A controlled input means React owns the value — the input shows what state says, and every keystroke updates state. React is always in control. Uncontrolled means the DOM owns the value — React only asks for it when needed."*

---

### Lesson 11 — Advanced Patterns
**Topic:** Real World React Architecture

**Concepts Covered:**
```
- Compound Components pattern
- Render Props pattern
- Higher Order Components (HOC) — know for exams
- Error Boundaries (class component you need to know)
- Suspense and lazy loading
- Code splitting
- Portal (modals rendered outside root)
- Forwarding Refs
```

**Karigar Co. Built:**
```
Modal system using Portals (cart drawer, image zoom)
Lazy loaded pages (Products, About load on demand)
Error Boundary wrapping product grid
Image with loading skeleton (Suspense-like pattern)
Compound component: custom Accordion for FAQ page
```

**Why You Won't Forget:**
> *"Patterns are solutions to problems that keep appearing. You don't need to memorize them — understand the PROBLEM each one solves, and you'll reach for the right pattern naturally."*

---

### Lesson 12 — Performance & Optimization
**Topic:** Making Karigar Co. Fast

**Concepts Covered:**
```
- React DevTools Profiler (hands on)
- What causes unnecessary re-renders?
- React.memo (component memoization)
- Virtualization (react-window for long lists)
- Image optimization strategies
- Bundle size analysis
- Key prop optimization
- Avoiding common performance traps
```

**Karigar Co. Built:**
```
Profiler audit of current app
Fix identified re-render issues with React.memo
Virtualized product list (100+ products)
Lazy loaded images with blur placeholder
Bundle analysis report
```

**Why You Won't Forget:**
> *"Optimization without measurement is guessing. Always profile FIRST, then fix what the data tells you. 90% of React performance issues are unnecessary re-renders."*

---

### Lesson 13 — Testing & Deployment
**Topic:** Confidence in Your Code, Ship It

**Concepts Covered:**
```
Testing:
  - Why test?
  - React Testing Library basics
  - Testing a component (render + interaction)
  - Testing a custom hook

Deployment:
  - Environment variables (.env)
  - Production build (npm run build)
  - Deploy to Vercel (frontend)
  - Configure for backend API URL
```

**Karigar Co. Built:**
```
Tests for:
  - ProductCard renders correctly
  - Cart add/remove works
  - Form validation triggers

Deployed to:
  - Vercel with custom domain setup
  - Environment variables configured
```

---

## PHASE 2: Node.js + Express + MongoDB (12 Lessons)

---

### Lesson 14 — Node.js Fundamentals
**Topic:** JavaScript on the Server

**Concepts Covered:**
```
- What is Node.js? (V8 engine, event loop)
- Node vs Browser JS (differences)
- Modules: require vs import
- Built-in modules: fs, path, http
- npm: packages, package.json, node_modules
- nodemon for development
- Environment variables with dotenv
```

**Karigar Co. Built:**
```
Backend folder structure created
Basic HTTP server (pure Node, no Express)
Understanding what Express replaces
```

---

### Lesson 15 — Express.js Fundamentals
**Topic:** Building APIs, Routing, Middleware

**Concepts Covered:**
```
- What is Express? Why use it?
- App setup and listen
- Routes: GET, POST, PUT, DELETE
- Request object (params, query, body)
- Response object (json, status, send)
- Middleware concept (the Express pipeline)
- Built-in middleware (express.json)
- Third-party middleware (cors, morgan)
- Error handling middleware
- Router (organizing routes in files)
```

**Karigar Co. Built:**
```
Express server running on port 5000
/api/products     GET  - return all products (mock)
/api/products/:id GET  - return one product
Basic middleware setup
CORS configured for React frontend
```

---

### Lesson 16 — MongoDB & Mongoose
**Topic:** NoSQL Database, Schema Design

**Concepts Covered:**
```
- SQL vs NoSQL (when to use which)
- MongoDB concepts: documents, collections
- Atlas setup (cloud MongoDB)
- Mongoose: connecting, schemas, models
- Schema types and validation
- CRUD operations with Mongoose
- Mongoose queries (.find, .findById, etc.)
```

**Karigar Co. Built:**
```
MongoDB Atlas cluster created
Product schema and model
Seed script (populate DB with 15 shirts)
Products API now reads from real MongoDB
```

---

### Lesson 17 — REST API Design
**Topic:** Professional API Architecture

**Concepts Covered:**
```
- REST principles
- Resource naming conventions
- HTTP status codes (which to use when)
- Request/Response cycle in detail
- API versioning (/api/v1/)
- Filtering, sorting, pagination
- API documentation mindset
```

**Karigar Co. Built:**
```
Full Products API:
  GET    /api/v1/products              (with filter, sort, paginate)
  GET    /api/v1/products/:slug        (single product)
  POST   /api/v1/products              (admin: create)
  PUT    /api/v1/products/:id          (admin: update)
  DELETE /api/v1/products/:id          (admin: delete)
```

---

### Lesson 18 — Authentication & JWT
**Topic:** Login System, Protected Routes

**Concepts Covered:**
```
- Authentication vs Authorization
- Password hashing with bcrypt
- JWT: what it is, how it works
- Sign token, verify token
- HTTP-only cookies vs localStorage
- Auth middleware (protect routes)
- Refresh tokens (concept)
```

**Karigar Co. Built:**
```
User model (name, email, password, role)
POST /api/v1/auth/register
POST /api/v1/auth/login
POST /api/v1/auth/logout
GET  /api/v1/auth/me (protected)
protect middleware
```

---

### Lesson 19 — Orders & Cart API
**Topic:** Core E-commerce Logic

**Concepts Covered:**
```
- Order schema design
- Cart: session vs database
- Transaction-like operations
- Stock management
- Order status flow
- Populate (Mongoose joins)
```

**Karigar Co. Built:**
```
Order model
Cart model
POST /api/v1/orders     (create order)
GET  /api/v1/orders/my  (user's orders)
PUT  /api/v1/orders/:id (update status - admin)
Stock decremented on order
```

---

### Lesson 20 — File Upload & Images
**Topic:** Handling Product Images

**Concepts Covered:**
```
- Multer for file uploads
- Cloudinary for image hosting
- Image optimization on upload
- Serving static files
```

**Karigar Co. Built:**
```
Product images uploaded to Cloudinary
Admin can update product images
Images served with proper URLs
```

---

### Lesson 21 — Advanced Mongoose
**Topic:** Aggregations, Virtuals, Middleware

**Concepts Covered:**
```
- Mongoose middleware (pre/post hooks)
- Virtual fields
- Aggregation pipeline
- Indexes for performance
- Population and references
```

**Karigar Co. Built:**
```
Review system (reviews embedded in products)
Average rating auto-calculated (virtual)
Pre-save hook: hash password, generate slug
Product search using text indexes
```

---

### Lesson 22 — Security & Best Practices
**Topic:** Production-Ready API

**Concepts Covered:**
```
- Rate limiting (express-rate-limit)
- Data sanitization (NoSQL injection)
- XSS protection (xss-clean)
- Security headers (helmet)
- CORS proper configuration
- Input validation (express-validator)
```

**Karigar Co. Built:**
```
All security middleware applied
Input validation on all routes
Rate limiting on auth routes
Production security checklist completed
```

---

### Lesson 23 — React + Backend Integration
**Topic:** Connecting Frontend to Backend

**Concepts Covered:**
```
- Axios setup and interceptors
- Centralized API service layer
- Handling auth tokens in React
- Protected routes on frontend
- Error handling across the stack
- Environment variables for API URL
```

**Karigar Co. Built:**
```
All mock data replaced with real API calls
Login/register actually works
Cart persists to database
Orders actually placed
Protected pages redirect if not logged in
```

---

### Lesson 24 — Admin Dashboard
**Topic:** Role-Based Features

**Concepts Covered:**
```
- Role-based access control
- Admin vs user routes
- Dashboard with real data
- Charts (recharts library)
```

**Karigar Co. Built:**
```
/admin route (protected, admin only)
Product management (CRUD from UI)
Order management
Basic sales stats
```

---

### Lesson 25 — Full Deployment
**Topic:** Taking Karigar Co. Live

**Concepts Covered:**
```
- Frontend: Deploy to Vercel
- Backend: Deploy to Railway/Render
- MongoDB Atlas production cluster
- Environment variables in production
- Custom domain setup
- CI/CD basics (auto-deploy on push)
- Post-launch checklist
```

**Karigar Co. Built:**
```
🚀 karigar.co is LIVE on the internet
Full MERN stack running in production
```

---

## The Complete Roadmap At a Glance
```
REACT FRONTEND (Lessons 1-13)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
L1  ✅ Setup, JSX, Virtual DOM        → Foundation built
L2  ⏳ Components & Props             → Product cards
L3     useState & Events              → Interactivity
L4     useEffect & APIs               → Real data
L5     React Router v6               → Navigation
L6     useContext                     → Global cart
L7     useReducer                     → Cart logic
L8     useRef, useMemo, useCallback   → Performance hooks
L9     Custom Hooks                   → Reusable logic
L10    Forms & Validation             → Checkout
L11    Advanced Patterns              → Architecture
L12    Performance & Optimization     → Speed
L13    Testing & Deployment           → Ship it

NODE.JS + EXPRESS + MONGODB (Lessons 14-25)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
L14    Node.js Fundamentals           → Server foundation
L15    Express.js                     → API routes
L16    MongoDB & Mongoose             → Database
L17    REST API Design                → Professional APIs
L18    Authentication & JWT           → Login system
L19    Orders & Cart API              → E-commerce core
L20    File Upload & Images           → Product images
L21    Advanced Mongoose              → Reviews, search
L22    Security & Best Practices      → Production ready
L23    React + Backend Integration    → Full stack!
L24    Admin Dashboard                → Management
L25    Full Deployment                → 🚀 LIVE

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
TOTAL: 25 lessons → Complete MERN developer
       1 project  → Real e-commerce platform
       1 portfolio piece → Gets you hired
```

---

## What Karigar Co. Looks Like When Complete
```
Features shipped:
✅ Beautiful product catalog
✅ Filter by category, size, color
✅ Search with debounce
✅ Product detail page
✅ Shopping cart (persists on refresh)
✅ Wishlist
✅ User authentication
✅ Checkout flow
✅ Order history
✅ Admin dashboard
✅ Image management
✅ Mobile responsive
✅ Fast and optimized
✅ Secure
✅ Deployed and live